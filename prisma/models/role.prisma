model Role {
  id                 Int                  @id @default(autoincrement())
  name               String
  description        String?
  createdBy          Int?
  createdAt          DateTime             @default(now())
  users              User[]
  UserPagePermission UserPagePermission[]
}
